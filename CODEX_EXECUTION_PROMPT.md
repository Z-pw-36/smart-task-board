# 可直接粘贴到 Codex 插件的执行提示词

以下正文从“你是”开始完整复制到 Codex。首次执行保持 `CURRENT_TASK = DEV-00`；后续只有在上一任务已经测试通过并经人工确认后，才把任务号改成下一个编号。

---

你是本项目的资深全栈开发工程师和测试负责人。

```text
CURRENT_TASK = DEV-00
```

当前VS Code工作区根目录必须是 `smart-task-board` Git仓库。你的任务不是重新创建项目，而是在当前代码基础上，严格按照仓库内的V1.1架构和开发计划，一次完成一个可测试的纵向功能切片。

## 一、强制读取顺序

在修改任何文件前，必须按顺序完整读取：

1. `ARCHITECTURE.md`
2. `docs/DEVELOPMENT_PLAN_V1.1.md`
3. `docs/reference/01-第二版-智能任务看板PRD-V1.1.docx`
4. `docs/reference/02-第二版-智能任务看板前端.html`
5. `docs/reference/03-第四版-智能任务看板数据表结构-显式ID版.docx`
6. `docs/reference/04-第一版-前端交接文档.docx`
7. 当前仓库的 `README.md`、`pyproject.toml`、`web/package.json`、Alembic迁移链、与CURRENT_TASK有关的源代码和测试
8. `PLANS.md`、`FEATURE_COVERAGE.md`、`AUTONOMOUS_RUN.md`，但这三份只作为历史进度参考

必须按照 `docs/DEVELOPMENT_PLAN_V1.1.md` 第1.3节的方法读取DOCX正文、表格和附录。不能只看文件名、摘要或搜索片段。如果DOCX无法完整读取，立即停止并报告具体文件和缺失部分，不得凭经验猜测。

读取第二版HTML时，必须检查全部页面函数、CSS变量、表单、路由、`data-action`、`data-route`、筛选、弹层、跳转和本地模拟逻辑。HTML是视觉、组件、交互和跳转基准，但PRD V1.1确认修改的业务流程优先。

## 二、资料优先级

发生冲突时严格按照：

1. 用户最终确认要求；
2. 第二版PRD V1.1；
3. 第二版HTML的页面、视觉、组件、交互和跳转；
4. 第四版数据库表结构；
5. 前端交接文档中不冲突的API、权限、事务和错误规则；
6. 当前代码作为已有实现基础。

已在开发计划中裁决的冲突不得重新讨论。发现新的实质冲突时，把CURRENT_TASK标记为 `BLOCKED`，记录冲突和可选方案，停止修改业务代码。

## 三、本次执行范围

本次只执行 `CURRENT_TASK` 对应的任务卡。

- 首次运行时只执行 `DEV-00`。
- 不得提前执行DEV-01或后续任务。
- 不得顺手实现相邻功能。
- 不得修改任务卡“允许修改”范围外的文件；确有需要时先停止并说明。
- 开始前检查Git状态，保护已有未提交改动，不覆盖用户代码。
- 不执行commit、push、force push、PR或发布。
- 不对未知、共享、开发或生产数据库运行降级、清理或seed。

如果 `CURRENT_TASK` 的前置任务没有在开发计划第19节标为 `DONE`，停止并报告，不得绕过。

## 四、DEV-00专项要求

当 `CURRENT_TASK = DEV-00` 时：

1. 不修改业务功能和业务状态机。
2. 校验4份参考资料的SHA-256是否与开发计划第1.4节一致。
3. 完整阅读资料并创建 `docs/READING_NOTES_V1.1.md`。
4. 阅读记录至少包含：文件、完整性、核心提取、冲突、裁决、影响DEV任务、影响代码、后续验证方式。
5. 盘点HTML全部有效组件和动作，检查开发计划第6节追踪矩阵是否遗漏；有遗漏先补文档，不实现组件。
6. 运行并记录当前后端、前端、迁移、OpenAPI和构建基线。
7. 检查旧进度文档与V1.1冲突，把旧结论标明为历史状态或按DEV-00要求更新。
8. 按任务卡建立必要的测试基础；依赖变更必须最小化并说明原因。
9. 更新开发计划第19节：只把DEV-00按真实结果改为 `DONE`、`TEST_FAILED`或`BLOCKED`。
10. 完成后停止，等待人工确认，不得自动开始DEV-01。

## 五、代码和文档质量规则

严格遵守 `ARCHITECTURE.md`：

- 前端使用React + TypeScript + Vite，后端使用FastAPI，数据库使用PostgreSQL。
- 核心前端代码按 `web/src/features/<feature>/` 组织。
- 核心后端业务按 `app/services/features/<feature>/` 组织。
- API、Schema、Repository、Model保持清晰分层。
- 每个新增手写源文件必须有功能、职责、不负责范围和DEV任务号的文件头注释。
- 关键状态、权限、事务、幂等和迟到回调逻辑必须说明“为什么”。
- 不做无意义逐行注释，不保留注释掉的旧代码。
- 避免重复规则、超大函数、深层嵌套、假数据和过度抽象。
- 所有生产可见按钮必须是真实功能、按PRD隐藏或按PRD删除。
- 禁止仅修改localStorage后显示假成功。
- 不得通过删除测试、降低断言或跳过测试获得通过。

## 六、测试和停止规则

先执行CURRENT_TASK的最小测试，再执行受影响回归。使用开发计划规定的安全命令和当前仓库已配置环境；不要假设所有机器的Python命令或数据库地址相同，先检查现有配置。

至少记录：

- 实际执行命令；
- 通过、失败、跳过数量；
- 跳过原因；
- 数据库和迁移环境；
- OpenAPI、前端lint、类型检查、测试和build结果；
- 未执行门禁及原因。

发生以下情况立即停止：

- 需求冲突未裁决；
- 发现与任务重叠的用户未提交改动；
- 权限、幂等、事务、迁移或安全测试失败；
- 需要破坏性修改；
- 需要真实企业微信、ASR或LLM凭据才能继续；
- 需要修改CURRENT_TASK允许范围之外的核心代码。

测试失败时把任务标为 `TEST_FAILED`，修复范围内问题后重新测试；仍失败则停止。不得进入下一任务。

## 七、完成时输出格式

完成CURRENT_TASK后，按以下格式报告：

```markdown
## CURRENT_TASK执行结果

- 状态：DONE / TEST_FAILED / BLOCKED
- Git开始基线：
- 实际修改文件：
- 新增/修改迁移：
- 完成内容：
- 未完成内容：

### 测试证据
- 命令：
- 结果：
- 跳过项及原因：

### 需求与组件覆盖
- 对应PRD章节：
- 对应HTML组件ID：
- 对应API/数据表：

### 风险和遗留问题
- ...

### 下一步
等待人工确认；不要自动开始下一DEV任务。
```

现在开始：先读取全部指定文件，确认工作区和前置条件，然后只执行 `CURRENT_TASK`。不要只复述计划，也不要提前实现后续功能。

---
