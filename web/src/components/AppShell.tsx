/**
 * Feature: Legacy AppShell compatibility export.
 * Responsibilities: keep existing imports pointed at the DEV-02 app-layer shell.
 * Does not own: route mapping, business pages, or shared component primitives.
 * Plan task: DEV-02.
 */

export { AppShell } from "../app/AppShell";
