/**
 * Feature: V1.1 workbench public entry.
 * Responsibilities: export the Workbench route component for app-layer routing.
 * Does not own: route guards, shared primitive internals, or later task overview/detail features.
 * Plan task: DEV-03.
 */

export { WorkbenchPage } from "./WorkbenchPage";
